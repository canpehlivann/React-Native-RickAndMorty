import { StyleSheet } from 'react-native';

export const navigationStyleSheet = StyleSheet.create({
  container: {
    paddingTop: 24, // Adjust this value as needed to accommodate status bar
  },
});